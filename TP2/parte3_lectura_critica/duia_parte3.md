# DUIA — Parte 3: Ejercicio de lectura crítica

| Campo | Completar |
|---|---|
| Herramienta | Claude (Claude Sonnet 5, claude.ai, chat) |
| Spec o prompt utilizado | Se subió el PDF de la consigna del TP2 ("Concurrencia e IA como motor primario") y el zip del proyecto propio (Food Store), y se pidió: "Antes de ejecutar, identificá qué haría realmente cada uno de los dos scripts SQL dados (`UPDATE funcion SET activa=FALSE;` y `DELETE FROM categoria WHERE id NOT IN (SELECT categoria_id FROM producto);`), por qué no cumple la consigna que dice cumplir, y escribí la versión corregida." |
| Qué generó | El análisis completo en `ejercicio_lectura_critica.md`: para el Script 1, que un `UPDATE` sin `WHERE` afecta todas las filas de la tabla, no solo las "retiradas de cartel"; para el Script 2, la trampa de `NOT IN` con `NULL` (si una sola fila de la subconsulta es `NULL`, la condición completa evalúa `UNKNOWN` para todas las filas y el `DELETE` no borra nada, en silencio); y las dos versiones corregidas: con `WHERE fecha_fin < CURRENT_DATE` para el primero, y con `NOT EXISTS` en vez de `NOT IN` para el segundo (más una variante opcional respetando el patrón de borrado lógico del proyecto propio). |
| Qué se aceptó | Todo el análisis se aceptó tal cual lo generó la IA, sin modificaciones: el razonamiento sobre `UPDATE` sin `WHERE` y sobre la semántica de tres valores de SQL (`TRUE`/`FALSE`/`UNKNOWN`) sobre la que se apoya el problema de `NOT IN` con `NULL` es correcto independientemente del esquema específico, y se verificó releyendo la documentación de PostgreSQL sobre `NOT IN` vs `NOT EXISTS`. |
| Qué se modificó o descartó, y por qué | No hubo correcciones de fondo. Se evaluó si hacía falta el nombre exacto de columna del "esquema genérico de cátedra" (tabla `funcion`, de los documentos teóricos de Semana 1/2 — un archivo distinto al manual de Food Store y al zip, y que no fue entregado) para cerrar la corrección del Script 1. Se descartó esa necesidad: lo que evalúa el ejercicio es identificar la ausencia de condición de filtro y proponer una condición plausible que exprese "retirada de cartel", no adivinar el nombre literal de una columna de un esquema ilustrativo que no vimos. Por eso `fecha_fin` se mantiene como nombre de ejemplo, con la aclaración explícita en el archivo de que debe ajustarse al nombre real si difiere — eso ya cubre el caso, sin necesidad de más información. |
| Verificación realizada | Esta parte no depende de conectarse a una base: es análisis directo de SQL, verificado de forma lógica y no contra un motor en ejecución: (1) `UPDATE ... SET activa=FALSE;` sin `WHERE` no tiene ninguna condición que filtre filas — por definición del lenguaje SQL, afecta a la tabla entera; esto no depende de datos ni de motor, es sintaxis. (2) El comportamiento de `NOT IN` con `NULL` se verificó releyendo la semántica de tres valores de SQL documentada por PostgreSQL: `x NOT IN (a, b, NULL)` nunca es `TRUE` para ningún `x`, es `FALSE` o `UNKNOWN`, y una fila con condición `UNKNOWN` en el `WHERE` no se selecciona. Como verificación adicional (opcional, no ejecutada por no tener motor disponible en esta sesión): crear una tabla de prueba con una fila `categoria_id IS NULL` en `producto` y comprobar en psql que el `DELETE` original efectivamente borra 0 filas mientras que la versión con `NOT EXISTS` sí borra las categorías correctas. |
