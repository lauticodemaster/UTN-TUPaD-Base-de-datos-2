### DUIA.
| Herramienta (IA) | Para qué se usó | Prompt/spec (resumen) | Se aceptó/rechazó porque |
| :--- | :--- | :--- | :--- |
| ChatGPT | Sugerir índices básicos para la consulta inicial. | "Actua como un DBA sr en PostgreSQL. Analisa la siugiente consulta y el esquema adjunto. Proveeme los comandos CREATE INDEX necesarios para optimizar el JOIN y el orrdenamiento. No alteres la cnosulta original, solo dame estrategias de indexacion b-tree estandar." | Se aceptaron los índices compuesto y simple. Se rechazó indexar `eliminado` y `disponible` porque el EXPLAIN evidenció baja cardinalidad. |
| Gemini | Optimizar estructura de la consulta y sugerir índices avanzados. | "Tengo un problema de rendimiento con una query pesada. Teniendo en cuanta que los campos 'eliminado' y 'disponible' estan casi siempre en los mismos estados, que tecnica exite en postgres para evitar indexar toda la tabla? Reescribe la consulta usando CTEs para pre-filtrar, ignora custiones de aplicacion." | Se aceptó el índice parcial y el CTE pre-filtrado por mejorar I/O. Se rechazó desnormalizar la tabla (sugerido por IA) por violar las reglas de la cátedra. |
| Claude | Buscar Index Only Scan y técnicas avanzadas de PostgreSQL. | "Necsito optmizar a nivel extremo la lectura de esta consulta en Postgres 15. Diseña un indice que me permita lograr un Index Only Scan para que el planificador no toque la tabla fisica. Dame tambien opciones de reescritura avanzadas usando join laterales, priorisando el costo de i/o." | Se aceptó el Covering Index (INCLUDE) por lograr el mejor tiempo. Se rechazó la reescritura con LATERAL JOIN porque empeoró el tiempo a 6000ms con un Nested Loop ineficiente. |
