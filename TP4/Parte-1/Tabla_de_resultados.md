## Tabla de resultados
| Consulta | Algoritmo de join (antes) | Cambio aplicado | Algoritmo de join (despues) | Mejora |
|--|--|--|--|--|
| Ranking de usuarios por gasto acumulado <br> `SELECT u.id, u.nombre u.apellido AS usuario, SUM(ped.total) AS gasto, RANK() OVER (ORDER BY SUM(ped.total) DESC) AS puesto FROM pedido ped JOIN usuario u ON u.id = ped.usuario_id WHERE ped.eliminado = FALSE AND u.eliminado = false GROUP BY u.id, u.nombre, u.apellido ORDER BY puesto;` | Hash Join <br> `Seq Scan` sobre `pedido` (200k) + `Hash` sobre `usuario` (20k) + `HashAggregate` + `WindowAgg`/`Sort`. Usa `idx_pedido_usuario_id` sin filtro, debe filtrar `eliminado` en heap y leer `total` de la tupla. | 1) Índice parcial covering: `CREATE INDEX idx_pedido_vigente_usuario_total ON pedido(usuario_id) INCLUDE (total) WHERE eliminado=FALSE;` y `CREATE INDEX idx_usuario_vigente ON usuario(id) WHERE eliminado=FALSE;` <br> 2) Reescritura: pre-agregar antes del JOIN `SELECT ... FROM (SELECT usuario_id, SUM(total) AS gasto FROM pedido WHERE eliminado=FALSE GROUP BY usuario_id) g JOIN usuario u ON u.id=g.usuario_id WHERE u.eliminado=FALSE ...` <br> 3) `ANALYZE` | Hash Join (se mantiene) pero optimizado <br> `Index Only Scan` sobre `pedido` vía índice parcial + `Hash Join` de 20k x 20k (antes 200k x 20k). Menor costo/buffers, sin acceso al heap. | Media (~30-40% estimado) <br> Reduce I/O y memoria del Hash (evita spill), baja `Execution Time`. Ganancia real si `VACUUM` mantiene `visibility map` para Index-Only. Sin filtro selectivo el Hash sigue siendo el algoritmo correcto. |
 | Facturación por categoría y por mes <br> `SELECT c.nombre AS categoria, date_trunc('month', ped.fecha)::DATE AS mes, SUM(dp.subtotal) AS facturado FROM detalle_pedido dp JOIN pedido ped ON ped.id = dp.pedido_id AND ped.eliminado = false JOIN producto pr ON pr.id = dp.producto_id JOIN categoria c ON c.id = pr.categoria_id WHERE dp.eliminado = FALSE AND c.eliminado = false GROUP BY c.nombre, date_trunc('month', ped.fecha) ORDER BY mes DESC, facturado DESC;` | Cadena de Parallel Hash Join <br> `Parallel Seq Scan` sobre `detalle_pedido` (600k) y `pedido` con `Filter: NOT eliminado` + `Hash` sobre `producto`/`categoria` + `Partial HashAggregate`/`Sort`. Sin índices covering, lee 100% de filas. | 1) Índices covering parciales: `CREATE INDEX idx_detalle_cover ON detalle_pedido(pedido_id, producto_id) INCLUDE (subtotal) WHERE eliminado=FALSE;` + `CREATE INDEX idx_producto_categoria ON producto(categoria_id);` <br> 2) Índice ya probado en TP3 `idx_pedido_fecha ON pedido(fecha) WHERE eliminado=FALSE` (solo útil con filtro por rango) <br> 3) Alternativa: `MATERIALIZED VIEW` mensual si el reporte es frecuente | Parallel Hash Join (se mantiene) <br> Idealmente cambia `Seq Scan` por `Index Only Scan`/`Bitmap Heap Scan` sobre los índices covering. El algoritmo no cambia porque la consulta agrupa casi toda la tabla; el `Hash Join` sigue siendo el más barato. | Baja / nula sin filtro (~10-15% solo por evitar heap) <br> Como verifica TP3 Parte 2, sin `WHERE` por rango de fecha el índice no se usa: el `Seq Scan` es más barato para leer todo. Mejora real solo si se agrega filtro `WHERE ped.fecha BETWEEN ...` o se usa vista materializada. |
